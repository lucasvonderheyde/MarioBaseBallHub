{
  "name": "mario-baseball",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "start": "cd web && npm run start:prod",
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "lint": "cd web && npm run lint",
    "stop": "cd web && npm run stop",
    "db:init": "cd web && npm run db:init",
    "db:push": "cd web && npm run db:push",
    "db:seed": "cd web && npm run db:seed",
    "db:bootstrap": "cd web && npm run db:bootstrap",
    "db:backfill": "cd web && npm run db:backfill",
    "setup": "bash scripts/setup-local.sh",
    "setup:railway": "bash scripts/railway-setup.sh",
    "test": "cd web && npm run test"
  }
}
