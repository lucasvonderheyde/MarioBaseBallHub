# Dependencies
node_modules/

# Railway CLI link (local)
.railway/

# OS / editor
.DS_Store

# Local SQLite (under web/data when developing), including backup snapshots
web/data/

# Production DB checkpoints (manual downloads — never commit)
data/checkpoints/
