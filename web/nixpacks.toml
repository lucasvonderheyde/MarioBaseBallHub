# Pin Node 22 (Next 16 + better-sqlite3 require 20+).
# Include all default nixpacks packages — overriding nixPkgs replaces the list entirely.
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x", "openssl", "python3", "gcc"]
